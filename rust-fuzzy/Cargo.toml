[package]
name = "fuzzy_search"
version = "0.1.0"
edition = "2021"
description = "Rust WebAssembly fuzzy search helpers for Listen1."

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"
